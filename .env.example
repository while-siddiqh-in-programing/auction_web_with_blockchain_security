# Example environment variables for docker-compose or local runs
# MongoDB connection string used by the Spring Boot backend
SPRING_DATA_MONGODB_URI=mongodb://mongo:27017/auctiondb
# Backend and frontend ports (frontend served by nginx when using docker-compose)
BACKEND_PORT=8080
FRONTEND_PORT=3000
# Any secret keys or API keys should be provided via environment variables in production
#JWT_SECRET=your_jwt_secret
